import React from "react";

/** 
 * * Components Material-ui */
import Typography from '@material-ui/core/Typography';

const Home = () => {
        return(
            <Typography aling="center" variant="h4">
                    Welcome Home!
            </Typography>
        );
}

export default Home;